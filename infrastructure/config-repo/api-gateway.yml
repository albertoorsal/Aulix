server:
  port: 8080

spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: false
          routes:
            - id: student-service
              uri: lb://student-service
              predicates:
                - Path=/api/students/**
            - id: staff-service
              uri: lb://staff-service
              predicates:
                - Path=/api/staff/**
            - id: teacher-service
              uri: lb://teacher-service
              predicates:
                - Path=/api/teachers/**
            - id: subject-service
              uri: lb://subject-service
              predicates:
                - Path=/api/subjects/**
            - id: auth-service
              uri: lb://auth-service
              predicates:
                - Path=/api/auth/**
            - id: user-service
              uri: lb://auth-service
              predicates:
                - Path=/api/users/**
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics,circuitbreakers,circuitbreakerevents,gateway

security:
  jwt:
    jwk-set-uri: ${AUTH_SERVICE_JWK_URI:http://localhost:9000/oauth2/jwks}

resilience4j:
  circuitbreaker:
    instances:
      studentServiceCircuitBreaker:
        base-config: default
      staffServiceCircuitBreaker:
        base-config: default
      teacherServiceCircuitBreaker:
        base-config: default
      subjectServiceCircuitBreaker:
        base-config: default
      notificationServiceCircuitBreaker:
        base-config: default
